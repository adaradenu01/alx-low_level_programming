#include <stdio.h>
/**
 * main - lowercase letters in reversed form
 *
 * Return: 0
*/

int main(void)
{
	char x;

	x = 'z';
	while
		(x >= 'a') {
			putchar(x);
			x--;
		}
	putchar('\n');
	return (0);
}
